services:
  - type: worker
    name: stopwatch-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: DATABASE_NAME
        value: stopwatch.db
      - key: TIMEZONE
        value: UTC
